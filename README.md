# To set up the environment, first install all requirements (requirements.txt) by running this command the following command
pip install -r requirements.txt --no-index --find-links file:///tmp/packages
